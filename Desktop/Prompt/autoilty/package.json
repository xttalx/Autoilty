{
  "name": "autoilty-marketplace",
  "version": "1.0.0",
  "description": "Full-stack marketplace for automotive detailing supplies",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server/index.js",
    "client": "cd client && npm start",
    "build": "cd Desktop/Prompt/autoilty/client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install",
    "vercel-build": "cd Desktop/Prompt/autoilty/client && npm install && npm run build"
  },
  "keywords": ["marketplace", "automotive", "detailing"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "stripe": "^14.0.0",
    "@supabase/supabase-js": "^2.38.4",
    "express-validator": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "concurrently": "^8.2.2"
  }
}
